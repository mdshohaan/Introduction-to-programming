#include <math.h>
#include <stdio.h>

int main() {
  // int ans = ceil(4.3);// 5
  // int ans = floor(4.3); // 4
  // int ans = round(4.3); // 4
  // int ans = floor(4.5); // 5
  // double ans = sqrt(20); // 4.472136
  // int ans =abs(-10); // 10
  int ans = pow(3, 2); // 9
  printf("%d", ans);
  return 0;
}